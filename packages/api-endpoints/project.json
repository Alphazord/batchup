{
  "name": "@repo/api-endpoints",
  "sourceRoot": "packages/api-endpoints/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run --passWithNoTests",
        "cwd": "packages/api-endpoints"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx eslint src/",
        "cwd": "packages/api-endpoints"
      }
    }
  }
}
